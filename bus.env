# -------- Spring Cloud Bus / Kafka environment --------
# Tags for Confluent images
KAFKA_TAG=7.6.1
ZOOKEEPER_TAG=7.6.1

# Global Spring profile used by all services in override
SPRING_PROFILES_ACTIVE=local
#SPRING_PROFILES_ACTIVE=dev

# Config Server Git backend (set to your real config repo)
CONFIG_GIT_URI=https://github.com/your-org/your-config-repo.git
CONFIG_DEFAULT_LABEL=main

# Optional basic auth for config server
# CONFIG_USER=configuser
# CONFIG_PASS=configpass

# ===== Spring Cloud Bus (Kafka) env for local compose =====

# --- Config Server Git backend ---
#CONFIG_GIT_URI= file:///config-repo
#CONFIG_DEFAULT_LABEL= main

# If using Basic Auth on Config Server (uncomment and set both)
# CONFIG_USER= configuser
# CONFIG_PASS= configpass

# Optional encryption key (if you enable /encrypt and /decrypt)
# CONFIG_ENCRYPT_KEY= change-me

# You generally do NOT need to set these below; they are provided by compose override.
# SPRING_CLOUD_STREAM_KAFKA_BINDER_BROKERS= kafka:9092
# SPRING_CLOUD_STREAM_KAFKA_BINDER_ZK_NODES= zookeeper:2181